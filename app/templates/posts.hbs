{{page-title "Posts"}}

<h1>Posts</h1>

<p><LinkTo @route="posts.new">New Post</LinkTo></p>

<ul>
  {{#each this.model as |post|}}
    <li data-test-post>
      <LinkTo @route="post" @model={{post.id}}>
        {{post.title}}
      </LinkTo>
    </li>
  {{/each}}
</ul>

{{outlet}}